# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
addons:
  apt:
    packages:
    - unixodbc-dev
    - libcurl4-openssl-dev
r_github_packages:
  - cran/survival